import 'package:flutter/material.dart';

class GridMenuDua extends StatelessWidget {
  const GridMenuDua({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
